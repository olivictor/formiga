import translator from '../src/formiga-translator'

describe('formigaTranslator', () => {
  describe('#init', () => {
    it('checks for missing translations on the given i18n object', () => {
    })
  })
})
